[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
rich = { version = "*" }
typer = { version = "*" }
pyserial = { version = "*" }

[dev-packages]

[requires]
python_version = "3.11"
